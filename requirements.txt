# VoiceGuard Emergency Shutdown Service - Production Dependencies
# Updated with verified available package versions

# Core scientific computing
numpy>=1.24.0
scipy>=1.11.0

# Audio processing (PyAudio requires system libraries on Linux)
PyAudio>=0.2.11; sys_platform == "win32"
librosa>=0.10.1
webrtcvad>=2.0.10
SpeechRecognition>=3.10.0

# Web and networking
aiohttp>=3.9.0
requests>=2.31.0

# GUI framework - COMPATIBLE VERSIONS
PyQt6>=6.4.2,<6.5.0
PyQt6-tools>=6.4.2.3.3

# Windows integration (Windows only)
pywin32>=306; sys_platform == "win32"
psutil>=5.9.0

# Image processing
Pillow>=10.1.0

# Security and encryption
cryptography>=41.0.0

# Configuration and data
PyYAML>=6.0.0
packaging>=23.2

# Development and formatting
black>=24.4.0

# Logging
colorlog>=6.8.0

# Note: sqlite3 is built into Python and doesn't need installation
